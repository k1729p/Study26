package kp;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test to verify that the Spring application context loads successfully.
 * This ensures that the application starts without any critical configuration issues.
 */
@SpringBootTest
class ApplicationTests {

    /**
     * Verifies that the Spring application context loads without errors.
     * This method serves as a basic sanity check.
     */
    @Test
    void contextLoads() {
        // Intentionally left blank as this test only ensures context loading.
    }
}
